#include "main.h"

/*
 * _islower - looking for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(void)
{
	return (c >= 'a' && c <= 'z');
}
